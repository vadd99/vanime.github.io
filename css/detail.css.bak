/* ==========================================
   STYLESHEET KHUSUS DETAIL FILM (css/detail.css)
   ========================================== */

/* Space bawah agar tidak tertutup sticky mobile bar pada mobile */
body {
    padding-bottom: 75px;
}

/* Detail Hero Section */
.detail-hero {
    position: relative;
    width: 100%;
    min-height: 480px;
    display: flex;
    align-items: flex-end;
    padding-bottom: 2rem;
    margin-top: 64px; /* Menghindari navbar */
}

.detail-hero-bg {
    position: absolute;
    inset: 0;
    z-index: 0;
}

.detail-hero-bg img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center 20%;
    opacity: 0.65;
}

.detail-hero-gradient {
    position: absolute;
    inset: 0;
    background: linear-gradient(0deg, var(--vadd-bg) 0%, rgba(5,18,10,0.85) 60%, rgba(5,18,10,0.4) 100%);
}

.detail-hero-content {
    position: relative;
    z-index: 10;
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 1rem;
    width: 100%;
    text-align: center;
}

/* Judul Seri */
.series-logo-title {
    max-width: 420px;
    margin: 0 auto 1.25rem auto;
    font-family: 'Montserrat', sans-serif;
    font-size: 2.2rem;
    font-weight: 900;
    text-transform: uppercase;
    letter-spacing: -0.02em;
    text-shadow: 0 4px 20px rgba(0,0,0,0.9);
    color: #ffffff;
    line-height: 1.1;
}

.series-logo-title span {
    color: var(--vadd-primary);
    display: block;
    font-size: 1.1rem;
    letter-spacing: 0.08em;
    margin-top: 0.3rem;
    font-weight: 800;
}

/* Bar Metadata & Rating */
.meta-info-bar {
    display: flex;
    align-items: center;
    justify-content: center;
    flex-wrap: wrap;
    gap: 0.5rem 0.75rem;
    font-size: 0.85rem;
    color: #d1d5db;
    margin-bottom: 0.75rem;
}

.age-badge {
    background-color: var(--vadd-card);
    border: 1px solid rgba(96, 211, 129, 0.4);
    color: var(--vadd-light);
    padding: 0.1rem 0.4rem;
    border-radius: 0.25rem;
    font-weight: bold;
    font-size: 0.75rem;
}

.meta-dot {
    color: var(--vadd-primary);
    font-size: 0.4rem;
}

.rating-box {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.3rem;
    color: var(--vadd-gold);
    font-size: 0.95rem;
    margin-bottom: 1.5rem;
}

.rating-val {
    color: #ffffff;
    font-weight: bold;
    margin-left: 0.3rem;
}

/* Tombol Aksi */
.action-buttons-group {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    max-width: 480px;
    margin: 0 auto;
}

.btn-watch-main {
    flex: 1;
    background-color: var(--vadd-accent);
    color: #ffffff;
    font-weight: 800;
    padding: 0.85rem 1.5rem;
    border-radius: 0.375rem;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.6rem;
    text-decoration: none;
    font-size: 0.95rem;
    box-shadow: 0 4px 20px rgba(242, 115, 41, 0.5);
    transition: transform 0.2s, background-color 0.2s;
    text-transform: uppercase;
    letter-spacing: 0.03em;
}

.btn-watch-main:hover {
    background-color: #e06019;
    transform: translateY(-2px);
}

.btn-square-icon {
    width: 48px;
    height: 48px;
    background-color: rgba(17, 51, 30, 0.6);
    border: 1px solid var(--vadd-primary);
    color: var(--vadd-primary);
    border-radius: 0.375rem;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.1rem;
    transition: all 0.2s;
}

.btn-square-icon:hover {
    background-color: var(--vadd-primary);
    color: var(--vadd-bg);
}

.btn-square-icon.active {
    background-color: var(--vadd-accent);
    border-color: var(--vadd-accent);
    color: #ffffff;
}

/* Akses Sekunder */
.secondary-actions {
    display: flex;
    justify-content: center;
    gap: 3rem;
    margin-top: 1.5rem;
}

.action-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.4rem;
    color: #d1d5db;
    font-size: 0.75rem;
    font-weight: bold;
    letter-spacing: 0.05em;
    cursor: pointer;
    transition: color 0.2s, transform 0.2s;
}

.action-item i {
    font-size: 1.25rem;
    color: var(--vadd-primary);
}

.action-item:hover {
    color: var(--vadd-primary);
    transform: translateY(-2px);
}

/* Container Konten */
.content-container {
    max-width: 1280px;
    margin: 0 auto;
    padding: 1.5rem 1rem;
}


/* =========================================
   SINOPSIS & METADATA EKSTRA (REVISI FINAL)
   ========================================= */
.synopsis-box {
    position: relative;
    margin-bottom: 2.5rem;
}

.synopsis-content-wrapper {
    position: relative;
    /* Membatasi tinggi kira-kira 3 baris teks */
    max-height: 4.5em; 
    overflow: hidden;
    transition: max-height 0.4s ease-out;
    margin-bottom: 0.75rem;
}

/* Efek gradien bayangan saat tertutup agar terlihat keren */
.synopsis-content-wrapper::after {
    content: "";
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 2.5em;
    background: linear-gradient(to bottom, rgba(5,18,10,0), rgba(5,18,10,1));
    pointer-events: none;
    transition: opacity 0.3s ease;
}

/* Saat tombol diklik dan kelas expanded aktif */
.synopsis-content-wrapper.expanded {
    max-height: 1500px; /* Nilai besar agar semua detail muat */
}

/* Hilangkan bayangan gradien saat terbuka penuh */
.synopsis-content-wrapper.expanded::after {
    opacity: 0;
}

.synopsis-text {
    color: #d1d5db;
    font-size: 0.95rem;
    line-height: 1.6;
    margin-bottom: 1rem;
    display: block; /* Mencegah bentrok dengan line-clamp */
}

/* Kotak Detail Tambahan (Audio, Sub, dll) */
.extra-details {
    color: #9ca3af;
    font-size: 0.85rem;
    line-height: 1.7;
    margin-top: 1.5rem;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    padding-top: 1rem;
    opacity: 0;
    transition: opacity 0.3s ease-in;
}

.synopsis-content-wrapper.expanded .extra-details {
    opacity: 1;
}

.extra-details p {
    margin-bottom: 0.6rem;
}

.extra-details strong {
    color: #ffffff;
    font-weight: 700;
    margin-right: 0.4rem;
}

/* Tombol Lihat Lebih */
.btn-more-detail {
    color: var(--vadd-accent);
    font-weight: bold;
    font-size: 0.8rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    background: none;
    border: none;
    cursor: pointer;
    padding: 0;
    margin-top: 0.5rem;
    transition: color 0.2s;
}

.btn-more-detail:hover { color: #ffffff; }

.icon-arrow { 
    font-size: 0.7rem; 
    transition: transform 0.3s ease;
}

/* Memutar ikon panah saat div expanded */
.synopsis-content-wrapper.expanded + .btn-more-detail .icon-arrow {
    transform: rotate(180deg);
}
/* ========================================= */


/* Section Episode */
.episodes-header {
    display: flex; align-items: center; justify-content: space-between;
    margin-bottom: 1.25rem; border-bottom: 1px solid var(--vadd-card); padding-bottom: 0.75rem;
}

.season-dropdown {
    color: #ffffff; font-family: 'Montserrat', sans-serif;
    font-weight: bold; font-size: 1.15rem; display: flex; align-items: center; gap: 0.5rem;
    background: none; border: none; cursor: pointer; padding: 0;
}

.season-arrow { font-size: 0.88rem; color: var(--vadd-primary); }
.episode-controls { display: flex; gap: 1.25rem; color: #9ca3af; font-size: 1.1rem; }
.episode-controls i { cursor: pointer; transition: color 0.2s; }
.episode-controls i:hover { color: var(--vadd-primary); }

/* Episode List */
.episode-list { display: flex; flex-direction: column; gap: 1.25rem; }

.episode-card {
    display: flex; gap: 1rem; color: inherit; text-decoration: none;
    padding: 0.5rem; border-radius: 0.5rem; transition: background-color 0.2s;
}

.episode-card:hover { background-color: rgba(17, 51, 30, 0.4); }

.episode-thumb-box {
    position: relative; width: 140px; min-width: 140px; aspect-ratio: 16 / 9;
    border-radius: 0.375rem; overflow: hidden; background-color: var(--vadd-card);
    border: 1px solid transparent; transition: border-color 0.3s;
}

.episode-card:hover .episode-thumb-box { border-color: var(--vadd-primary); }
.episode-thumb-box img { width: 100%; height: 100%; object-fit: cover; transition: transform 0.3s; }
.episode-card:hover .episode-thumb-box img { transform: scale(1.08); }

.episode-play-overlay {
    position: absolute; inset: 0; background-color: rgba(0,0,0,0.3); display: flex;
    align-items: center; justify-content: center; opacity: 0; transition: opacity 0.2s;
}

.episode-card:hover .episode-play-overlay { opacity: 1; }
.episode-play-overlay i { color: #ffffff; font-size: 1.5rem; filter: drop-shadow(0 2px 5px rgba(0,0,0,0.8)); }

.episode-time-badge {
    position: absolute; bottom: 0.3rem; right: 0.3rem;
    background-color: rgba(5, 18, 10, 0.85); color: #ffffff;
    font-size: 0.7rem; font-weight: bold; padding: 0.1rem 0.35rem; border-radius: 0.2rem;
}

.episode-details { flex: 1; display: flex; flex-direction: column; justify-content: center; }
.episode-series-name { font-size: 0.65rem; color: #9ca3af; text-transform: uppercase; margin-bottom: 0.25rem; font-weight: 700; }
.episode-title { font-size: 0.9rem; font-weight: bold; color: #ffffff; margin-bottom: 0.35rem; line-height: 1.25; transition: color 0.2s; }
.episode-card:hover .episode-title { color: var(--vadd-primary); }
.episode-sub-info { font-size: 0.75rem; color: #9ca3af; }

.episode-more-btn { color: #9ca3af; font-size: 1rem; padding: 0 0.5rem; align-self: center; transition: color 0.2s; background: none; border: none; cursor: pointer; }
.episode-more-btn:hover { color: var(--vadd-primary); }

/* Sticky Mobile Bar */
.sticky-mobile-bar {
    position: fixed; bottom: 0; left: 0; width: 100%;
    background-color: var(--vadd-nav); border-top: 1px solid var(--vadd-card);
    padding: 0.75rem 1rem; display: flex; gap: 0.75rem; z-index: 40; box-shadow: 0 -4px 15px rgba(0,0,0,0.6);
}

.sticky-mobile-bar .btn-watch-main { padding: 0.65rem 1rem; font-size: 0.85rem; box-shadow: none; }
.sticky-mobile-bar .btn-square-icon { width: 42px; height: 42px; }

/* Toast Pop-up */
#vadd-toast {
    position: fixed; bottom: 80px; left: 50%; transform: translateX(-50%) translateY(100px);
    background-color: var(--vadd-card); color: var(--vadd-primary); border: 1px solid var(--vadd-primary);
    padding: 0.6rem 1.2rem; border-radius: 0.5rem; font-size: 0.85rem; font-weight: bold;
    box-shadow: 0 4px 15px rgba(0,0,0,0.5); z-index: 100; opacity: 0; transition: all 0.3s ease; pointer-events: none;
}

#vadd-toast.show { transform: translateX(-50%) translateY(0); opacity: 1; }

/* Media Queries Desktop */
@media (min-width: 768px) {
    .sticky-mobile-bar { display: none; }
    body { padding-bottom: 0; }
    .detail-hero { min-height: 520px; }
    .series-logo-title { font-size: 2.8rem; max-width: 600px; }
    .series-logo-title span { font-size: 1.3rem; }
    .episode-list { display: grid; grid-template-columns: repeat(2, 1fr); gap: 1.5rem; }
    .episode-thumb-box { width: 180px; min-width: 180px; }
    #vadd-toast { bottom: 30px; }
}
